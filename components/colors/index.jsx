import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(15, 120, 15)',
      mainApha: 'rgba(15, 120, 15, 0.5)'
    },
  },
});

export default theme;